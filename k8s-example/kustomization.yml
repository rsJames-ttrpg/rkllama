namespace: rkllama

resources:
  # Choose ONE: deployment.yml (single node) OR daemonset.yml (multi-node cluster)
  # - ./deployment.yml
  - ./daemonset.yml
  - ./ingress.yml
  - ./pv.yml
  - ./service.yml

  # Router (optional) - for multi-node deployments with model-aware routing
  - ./router

images:
  - name: ghcr.io/rsjames-ttrpg/rkllama
    newTag: 1.2.3-6
  - name: ghcr.io/rsjames-ttrpg/rkllama-router
    newTag: 3c2983d
